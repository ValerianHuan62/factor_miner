# Factor Miner 代理规则

本文件只保存 agent 不得违反的硬规则。人类运行方式写在 `README.md`，设计依据写在 `docs/`。

## 自动 Skill 与文档语言

- 每次处理本项目的量化研究、因子、数据、统计、测试或技术文档任务时，必须先完整读取并使用 `skills/quant_coding/SKILL.md`。
- 所有新增或修改的 Markdown 文档，其标题与正文必须使用中文；代码标识、命令、路径、标准缩写和专有名词可以保留原文。
- 向用户解释系统时，必须先说明金融研究过程、结论边界和风险，再说明程序实现。
- 项目内 Skill 只约束开发代理，不得成为核心包或 CLI 的运行时依赖；删除 Skill 后完整流程仍须可运行。

## 项目边界

- `factor_miner` 是独立 Python 项目；核心包和 CLI 不得 import `huan_quant`、个人 Skill、个人目录或个人数据库。
- CLI 是正式入口。未来 Skill 只能调用 CLI；删除 Skill 后完整流程仍须可运行。
- 当前项目包含：人工或受控 LLM 假设 → 类型化 AST 校验 → 确定性编译 → 真实数据计算 → 可见验证与组合诊断 → 不可变账本 → PostgreSQL 只读投影与 Dashboard。
- PostgreSQL 是可重建读模型，不得替代 JSON/JSONL 与不可变运行产物。
- 输出只能称为“通过当前协议验证的候选因子”，不得直接称为认证 Alpha、生产结论或实盘建议。

## 研究红线

- 候选必须在看到结果前冻结主张、机制、预期方向、可观察代理、独立验证方案、竞争解释、失效方式、证伪路径和来源。
- 经济叙事必须可独立验证；未执行独立机制检验时只能标记 `mechanism_unverified`。结果后解释不得覆盖事前假设。
- 引用必须可检索、可核验；无法核验的引用不得作为候选依据，禁止编造引用。
- 人工因子与未来 LLM 因子必须走相同的 Spec、compiler、evaluator 和统计协议。
- 禁止未来信息：negative shift、centered rolling、future field、future fill、标签泄漏和未来状态；基本面必须按可得日对齐。
- 只允许白名单类型化 AST；禁止 `eval`、`exec`、动态导入和任意 Python 因子代码。
- 原始因子与预处理严格分层。原始层不得做 MAD、缩尾、标准分、排名标准化、中性化或 `fillna(0)`。
- 评价协议、掩码、标签、切分、试验预算、统计检验族、HAC 参数和冗余阈值必须在结果揭晓前冻结，候选不得覆盖。
- 每个候选和参数变体占用预登记的假设名额；失败、中断和重复候选不得删除或从多重检验族中消失。
- RankIC 显著性必须处理时间依赖；使用冻结参数的 HAC，并以 Bonferroni 校正作为可见通过的显著性闸门。DSR 不得代替因子 IC 多重检验。
- 冗余先检查规范 AST 和哈希，再检查可见区间逐日截面 Spearman 输出相关性；高冗余候选保留记录但不通过。
- 缺数据版本、状态表、配置、账本或评价器时必须硬失败，不提供静默兜底。

## 跨市场数据执行边界

- 真实候选计算可在用户控制的 macOS、Linux 或其他受支持环境执行，不得把 SSH、特定服务器或个人目录作为运行前提。
- 正式上游是满足标准面板数据合同的显式 CSV/Parquet 发布；QuantLake 只是 A 股适配来源之一，不得成为核心包的强制依赖。
- A 股、美股或其他市场都必须显式提供或确认可计算、可排名、可交易 mask；系统不得猜测停牌、退市、涨跌停或交易时段语义。
- 输入发布必须只读使用；候选、账本和运行产物必须写入显式配置的独立产物根目录。
- 每次真实运行必须记录数据来源、发布版本、清单哈希、数据结构、截止日、复权口径、交易日历、状态表版本、代码提交和配置哈希。
- release 或复权口径未知、状态表落后、字段缺失、主键重复或截止日期不一致时必须失败。
- DolphinDB 已退出技术路线，不得新增适配器、依赖、服务或兜底。
- 私有原始数据、个股样本、完整结果、账本、模型和密钥不得进入 Git，也不得发送给外部大语言模型。

## 文件账本

- JSON/JSONL 是正式主存储，不是数据库失败时的 fallback。
- CandidateSpec 与 CampaignSpec 使用规范 JSON 和内容哈希，登记后不可原地修改。
- `trials.jsonl` 只允许追加；纠错必须追加带 `supersedes_event_id` 的新事件。
- 候选和批次必须在读取任何标签、IC、p 值或输出相关性前登记。
- 当前实现仅允许单机单写入器；检测到并发写入器、重复 ID、哈希不一致、截断或损坏时硬失败。
- 产物只保存结果，不得创建名为 Evidence 或 Conclusion 的产物。

## Dashboard 读模型硬规则

- PostgreSQL 中的候选业务编号必须使用全库稳定顺延的 `huanNNN`；正式槽位 ID 和内容寻址 candidate ID 只能保存在 `source_candidate_id` 等审计列，不得作为 Dashboard 主显示编号。
- 同一 `source_candidate_id` 必须永久复用同一 `huanNNN`；编号分配必须在数据库事务内串行执行，禁止按单次运行重新从 `huan001` 开始。
- Stage C 已评价候选写入 PostgreSQL 前必须具备非空的 `ic_mean`、`rank_ic_mean`、`ic_std`、`rank_ic_std`、`ic_ir`、`rank_ic_ir`、IC/RankIC HAC t 值、`win_rate`、年化收益、最大回撤、Sharpe 和 information ratio；任一缺失必须拒绝发布和投影，不得以 NULL 静默代替。
- `win_rate` 必须由冻结组合口径的逐期 `Q10_Q1_net_return` 计算；不得由年化收益、Sharpe 或累计收益反推。

## 工程规则

- 从第一性原理明确输入、输出、不变量和失败语义；主流程错误必须暴露并修正，不写掩盖错误的兜底实现。
- 成熟开源方案优先复用；采用前记录官方仓库、当时 star 数、最近 release 和维护状态。
- 代码、测试、配置结构、示例和文档进 Git；真实数据、运行配置、账本、产物、密钥和服务器路径解析结果不进 Git。
- 依赖由 `pyproject.toml` 与 `uv.lock` 固定；正式验证命令必须在承载目标数据发布的受控环境执行。
- 行为变更必须同步更新测试、数据合同和中文文档；完成前运行与风险相称的合成回归。
