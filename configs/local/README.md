# 本地私有配置说明

本目录保存不会进入 Git 的 `*.local.json`。这些文件可能包含研究授权、一次性候选批次或本机运行参数，不是程序代码，也不是正式不可变产物。

- 当前仍可能使用的外部研究政策放在本目录；
- 历史 V0.5 一次性输入放在 `legacy-v05/`；
- 新运行应优先通过 CLI 显式传入路径；
- 正式运行结果、账本和大面板写到显式独立产物根；本机统一使用 `artifacts/A股/` 或 `artifacts/美股/`，真实内容不进入 Git。

删除本目录前应先核对命令历史和不可变产物引用。程序不得把这里的文件作为默认运行时依赖。
